import {useState} from 'react';import {View,Text,Pressable,StyleSheet} from 'react-native';import * as ImagePicker from 'expo-image-picker';
export default function Documents(){const [msg,setMsg]=useState('');async function pick(){const result=await ImagePicker.launchImageLibraryAsync({mediaTypes:['images'],quality:.7});if(result.canceled)return;const asset=result.assets[0];setMsg(`Selected ${asset.fileName||'document photo'}. Demo mode keeps the selection local; connect Supabase Storage later for cloud upload.`)}return <View style={s.wrap}><Text style={s.title}>Shipment Documents</Text><Text style={s.muted}>Delivery, cargo, checkpoint and other relevant photos.</Text><Pressable style={s.btn} onPress={pick}><Text style={s.btnText}>Choose photo</Text></Pressable>{msg?<Text>{msg}</Text>:null}</View>}const s=StyleSheet.create({wrap:{flex:1,padding:20,gap:16,backgroundColor:'#f5f7fb'},title:{fontSize:26,fontWeight:'800'},muted:{color:'#687386'},btn:{backgroundColor:'#182337',padding:16,borderRadius:12,alignItems:'center'},btnText:{color:'white',fontWeight:'800'}})
