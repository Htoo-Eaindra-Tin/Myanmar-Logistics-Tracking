'use client';import {useEffect,useState} from 'react';import {getDemoState,shipmentWithRelations} from '../../../lib/demo'
export default function Shipments(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{const load=()=>{const s=getDemoState();setRows(s.shipments.map((r:any)=>shipmentWithRelations(s,r)).sort((a:any,b:any)=>b.created_at.localeCompare(a.created_at)))};load();window.addEventListener('logistics-demo-update',load);return()=>window.removeEventListener('logistics-demo-update',load)},[]);return <><h1>All Shipments</h1><div className="card"><table className="table"><thead><tr><th>Tracking</th><th>Cargo</th><th>Trader</th><th>Driver</th><th>Route</th><th>Status</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.tracking_number}</td><td>{r.cargo_description}</td><td>{r.profiles?.name}</td><td>{r.driver?.name}</td><td>{r.routes?.name}</td><td><span className="badge">{r.status}</span></td></tr>)}</tbody></table></div></>}
